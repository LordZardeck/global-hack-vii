import React, {Component} from 'react';
import {connect} from 'react-redux';

class VisaType extends Component {
    render() {
        return (
            <div>
                Goodbye
            </div>
        );
    }
}

export default connect()(VisaType);
